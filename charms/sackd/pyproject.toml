[project]
name = "sackd"
version = "0.0"
requires-python = "==3.14.*"
dependencies = [
    "ops",
    "charmed-hpc-libs[ops]",
    "cosl",
    "charmed-slurm-sackd-interface",
    "slurm-ops"
]

[tool.repository]
libraries = ["prometheus_k8s.prometheus_scrape"]
